apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  labels:
    app: app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      # REQUIRED: tolerate the spot taint AKS applies to spot nodes.
      tolerations:
        - key: "kubernetes.azure.com/scalesetpriority"
          operator: "Equal"
          value: "spot"
          effect: "NoSchedule"
      affinity:
        nodeAffinity:
          # REQUIRED: only run on NAP/Karpenter nodes (excludes the fixed agentpool).
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: karpenter.sh/capacity-type
                    operator: In
                    values: ["spot", "on-demand"]
          # PREFERRED: among NAP nodes, prefer spot.
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: karpenter.sh/capacity-type
                    operator: In
                    values: ["spot"]
      # No zone spread — lets Karpenter pack onto spot instead of forcing on-demand
      # into zones that lack spot capacity.
      containers:
        - name: app
          image: mafamafa/phpinfo-ubuntu:202401182132   # <-- your image:tag
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          ports:
            - containerPort: 80
      terminationGracePeriodSeconds: 25
